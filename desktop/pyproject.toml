[project]
name = "3dssync-desktop"
version = "0.1.0"
description = "Desktop client for Save Sync"
requires-python = ">=3.11"
dependencies = [
    "PyQt6>=6.5.0",
    "requests>=2.31.0",
    "paramiko>=3.0.0",
    "py7zr>=1.0.0",
]

[dependency-groups]
dev = [
    "pytest>=7.4.0",
]
